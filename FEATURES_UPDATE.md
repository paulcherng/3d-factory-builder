# 3D 工廠佈局編輯器 - 新功能更新

## 🎉 新增功能

### 1. 多選物件功能 ✨
- **Ctrl + 單擊**：在 3D 場景或列表中按住 Ctrl 鍵點擊可選擇多個物件
- **多選指示**：工具列會顯示已選擇的物件數量
- **列表多選**：在左側區域列表中也支援 Ctrl + 單擊多選

### 2. 物件對齊功能 📐
- **對齊 X 軸**：將所有選中的物件沿 X 軸對齊到第一個選中物件的位置
- **對齊 Y 軸**：將所有選中的物件沿 Y 軸對齊（高度對齊）
- **對齊 Z 軸**：將所有選中的物件沿 Z 軸對齊
- **要求**：至少選擇 2 個物件才能使用對齊功能

### 3. Transform Controls（類似 Tinkercad）🎯

#### 移動模式（Translate）
- 點擊工具列的「移動」按鈕啟用
- 選中物件後會顯示三個彩色箭頭（紅/綠/藍）
- **拖拉箭頭**：沿著 X/Y/Z 軸精確移動物件
- **拖拉平面**：在兩個軸組成的平面上移動
- 支援網格吸附功能

#### 縮放模式（Scale）
- 點擊工具列的「縮放」按鈕啟用
- 選中物件後會顯示三個彩色方塊控制點
- **拖拉方塊**：沿著 X/Y/Z 軸獨立縮放物件
- **拖拉中心**：等比例縮放物件
- 縮放步進：0.1 單位

### 4. 改善的拖拉體驗 🖱️
- 使用 React Three Drei 的 TransformControls
- 更流暢的拖拉操作
- 視覺化的軸向指示
- 即時預覽變換效果
- 支援網格吸附

### 5. 改進的選擇系統 🎨
- **黃色邊框**：選中的物件顯示黃色高亮邊框
- **多選支援**：可同時選擇和操作多個物件
- **點擊空白處**：清除所有選擇
- **視覺回饋**：清晰的選中狀態指示

## 🎮 操作指南

### 基本操作
1. **選擇物件**：單擊 3D 場景中的物件或列表項
2. **多選物件**：按住 Ctrl 鍵並單擊多個物件
3. **清除選擇**：點擊 3D 場景的空白處

### 移動物件
1. 點擊工具列的「移動」按鈕
2. 選擇要移動的物件
3. 拖拉彩色箭頭沿軸移動
4. 或拖拉平面在兩個軸上移動

### 縮放物件
1. 點擊工具列的「縮放」按鈕
2. 選擇要縮放的物件
3. 拖拉彩色方塊沿軸縮放
4. 或拖拉中心方塊等比例縮放

### 對齊物件
1. 按住 Ctrl 選擇多個物件（至少 2 個）
2. 點擊「對齊 X」、「對齊 Y」或「對齊 Z」按鈕
3. 所有物件會對齊到第一個選中物件的位置

## 🎨 UI 改進

### 工具列增強
- **變換模式切換**：移動/縮放按鈕（高亮顯示當前模式）
- **對齊按鈕組**：X/Y/Z 軸對齊按鈕
- **多選計數器**：顯示當前選中的物件數量
- **分隔線**：清晰的功能分組

### 幫助面板
- 右上角顯示操作提示
- 包含所有快捷鍵和操作說明
- 半透明背景，不遮擋視圖

### 屬性面板
- 多選時顯示提示信息
- 引導用戶使用對齊功能

## 🔧 技術實現

### 新增的 Store Actions
```typescript
- selectZone(id, multiSelect): 支援多選的選擇功能
- selectMultipleZones(ids): 批量選擇
- clearSelection(): 清除所有選擇
- alignZones(axis): 對齊功能
- setTransformMode(mode): 切換變換模式
```

### 新增的 State
```typescript
- selectedZoneIds: string[] // 多選 ID 列表
- transformMode: 'translate' | 'scale' // 變換模式
```

### 使用的庫
- **@react-three/drei**: TransformControls 元件
- 提供專業級的 3D 變換控制
- 支援多種變換模式和吸附功能

## 📊 與 Tinkercad 的相似性

✅ **已實現**：
- 三軸移動控制（紅/綠/藍箭頭）
- 三軸縮放控制（彩色方塊）
- 視覺化的變換控制器
- 網格吸附功能
- 多選和對齊

🔄 **未來可擴展**：
- 旋轉控制
- 複製/貼上
- 群組功能
- 撤銷/重做

## 🚀 使用建議

1. **精確定位**：使用移動模式的軸向箭頭進行精確定位
2. **快速對齊**：選擇多個物件後使用對齊功能快速排列
3. **尺寸調整**：使用縮放模式直接在 3D 場景中調整大小
4. **網格吸附**：保持網格吸附開啟以確保物件對齊到網格

## 🎯 效果展示

現在你可以：
- ✨ 像在 Tinkercad 中一樣直觀地操作 3D 物件
- 🎯 精確控制物件的位置和大小
- 📐 快速對齊多個物件
- 🖱️ 享受流暢的拖拉體驗

開始使用這些新功能來創建你的 3D 工廠佈局吧！🏭
