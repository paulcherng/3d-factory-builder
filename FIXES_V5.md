# 3D 工廠編輯器 - 功能更新 V5

## 已完成的功能

### 1. ✅ 移除設定面板中的文字大小預覽區塊
- 移除了「預覽：物體名稱」的預覽文字
- 保留了滑桿和數值顯示
- 介面更簡潔

### 2. ✅ 撤銷/重做功能
- **快捷鍵支持**：
  - `Ctrl+Z`: 撤銷
  - `Ctrl+Y` 或 `Ctrl+Shift+Z`: 重做
- **工具列按鈕**：
  - ↶ 撤銷按鈕
  - ↷ 重做按鈕
  - 按鈕會在無法操作時自動禁用
- **歷史記錄**：
  - 保留最近 50 個操作狀態
  - 自動記錄新增、刪除、顏色變更等重要操作

### 3. ✅ 修復 Shift+框選功能
- **問題修復**：
  - ✅ 框選時攝影機不再移動（按住 Shift 時禁用 OrbitControls）
  - ✅ 使用 Three.js 投影正確計算物體屏幕位置
  - ✅ 框選到的物件會被正確選中
- **使用方式**：
  - 按住 `Shift` 鍵
  - 在 Canvas 上拖曳滑鼠
  - 釋放滑鼠後，框內的物件會被選中

### 4. ✅ 群組功能
- **群組操作**：
  - 選擇多個物件後點擊「🔗 群組」按鈕
  - 快捷鍵：`Ctrl+G`
- **取消群組**：
  - 選擇群組中的物件後點擊「🔓 取消群組」按鈕
  - 快捷鍵：`Ctrl+Shift+G`
- **群組資料結構**：
  - 每個物件可以有 `groupId` 屬性
  - Store 中維護群組映射表

### 5. ✅ 物件顏色變更功能
- **顏色選擇器**：
  - 在屬性面板中新增顏色選擇器
  - 使用 HTML5 原生 `<input type="color">`
- **重置顏色**：
  - 提供「重置顏色」按鈕
  - 重置後使用預設的類型顏色
- **即時預覽**：
  - 顏色變更會立即反映在 3D 場景中
  - 自定義顏色會覆蓋預設的類型顏色

## 技術實現細節

### 資料結構更新
```typescript
// Zone 介面新增欄位
interface Zone {
  // ... 原有欄位
  color?: string;      // 自定義顏色
  groupId?: string;    // 群組 ID
}

// EditorState 新增欄位
interface EditorState {
  // ... 原有欄位
  history: Zone[][];           // 歷史記錄
  historyIndex: number;        // 當前歷史索引
  groups: { [groupId: string]: string[] }; // 群組映射
}
```

### Store 新增 Actions
- `saveHistory()`: 儲存當前狀態到歷史記錄
- `undo()`: 撤銷到上一個狀態
- `redo()`: 重做到下一個狀態
- `groupZones()`: 將選中的物件群組
- `ungroupZones()`: 取消選中物件的群組

### 組件更新
1. **Toolbar.tsx**：
   - 新增撤銷/重做按鈕
   - 新增群組/取消群組按鈕
   - 新增鍵盤快捷鍵監聽

2. **Canvas3D.tsx**：
   - 新增 Shift 鍵狀態監聽
   - 按住 Shift 時禁用 OrbitControls
   - 整合 SelectionBoxHelper

3. **SelectionBoxOverlay.tsx**：
   - 使用 Three.js Camera 進行正確的 3D 到 2D 投影
   - 修復框選邏輯
   - 防止框選時移動攝影機

4. **PropertyEditor.tsx**：
   - 新增顏色選擇器
   - 新增重置顏色按鈕

5. **Zone3D.tsx**：
   - 支持自定義顏色渲染
   - 優先使用 `zone.color`，否則使用類型預設顏色

6. **SettingsPanel.tsx**：
   - 移除文字大小預覽區塊

## 使用說明

### 撤銷/重做
- 進行任何編輯操作後，可以使用 `Ctrl+Z` 撤銷
- 撤銷後可以使用 `Ctrl+Y` 重做
- 或使用工具列的按鈕

### 框選多個物件
1. 按住 `Shift` 鍵
2. 在 3D 場景中拖曳滑鼠
3. 釋放滑鼠，框內物件會被選中

### 群組物件
1. 使用框選或 Ctrl+點擊選擇多個物件
2. 點擊「🔗 群組」按鈕或按 `Ctrl+G`
3. 群組後的物件會共享同一個 groupId

### 變更物件顏色
1. 選擇一個物件
2. 在右側屬性面板找到「顏色」選項
3. 點擊顏色選擇器選擇新顏色
4. 點擊「重置顏色」可恢復預設顏色

## 測試建議

1. **撤銷/重做測試**：
   - 新增幾個物件
   - 按 Ctrl+Z 撤銷
   - 按 Ctrl+Y 重做
   - 驗證物件正確出現/消失

2. **框選測試**：
   - 新增多個物件
   - 按住 Shift 拖曳框選
   - 驗證攝影機不移動
   - 驗證框內物件被選中

3. **群組測試**：
   - 框選多個物件
   - 點擊群組按鈕
   - 驗證物件被群組
   - 點擊取消群組按鈕驗證解除群組

4. **顏色測試**：
   - 選擇一個物件
   - 變更顏色
   - 驗證 3D 場景中顏色即時更新
   - 點擊重置驗證恢復預設顏色
