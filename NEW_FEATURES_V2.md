# 3D 工廠佈局編輯器 - 第二波功能更新 🚀

## ✨ 新增功能

### 1. 圓柱體支援 🎯
- **新增圓柱體按鈕**：工具列新增「➕ 圓柱體」按鈕
- **圓柱體參數**：
  - 半徑（Radius）
  - 高度（Height）
  - 分段數（Segments）- 控制圓柱的平滑度
- **完整支援**：圓柱體支援所有變換操作（移動、縮放、對齊）

### 2. 改善的拖拉體驗 🖱️
- **禁用攝影機移動**：在使用 Transform Controls 拖拉物體時，攝影機不會跟著移動
- **流暢操作**：變換時自動禁用 OrbitControls
- **即時回饋**：變換開始和結束時有明確的狀態切換

### 3. 物體名稱標籤 🏷️
- **3D 標籤**：物體名稱顯示在物體上方
- **動態定位**：標籤位置根據物體高度自動調整
- **選中高亮**：選中的物體標籤有黃色邊框
- **半透明背景**：黑色半透明背景確保可讀性
- **預覽和編輯模式**：兩種模式下都顯示名稱

### 4. 框選功能 📦
- **Shift + 拖拉**：按住 Shift 鍵並拖拉滑鼠創建選擇框
- **視覺回饋**：藍色虛線框和半透明填充
- **批量選擇**：框選範圍內的所有物體
- **智能檢測**：基於物體在螢幕上的投影位置

### 5. 點擊選擇修正 ✅
- **直接點擊**：現在可以直接點擊物體外型進行選擇
- **Transform Controls 不干擾**：點擊物體本身即可選擇
- **多選支援**：Ctrl + 點擊仍然有效

### 6. XYZ 軸指示器 📐
- **AxesHelper**：場景中顯示紅（X）、綠（Y）、藍（Z）三色座標軸
- **20 單位長度**：清晰的方向指示
- **始終可見**：幫助理解空間方向

### 7. ViewCube 視角控制 🧊
- **右下角方塊**：類似 Tinkercad 的視角控制方塊
- **點擊切換視角**：
  - 前、後、左、右
  - 上、下
  - 各種角度組合
- **視覺回饋**：懸停時高亮顯示（藍色）
- **快速導航**：一鍵切換到標準視角

## 🎮 詳細操作指南

### 創建圓柱體
1. 點擊工具列的「➕ 圓柱體」按鈕
2. 新圓柱體會出現在場景中心
3. 在屬性面板中調整：
   - **半徑**：控制圓柱的粗細
   - **高度**：控制圓柱的高度
   - **分段**：控制圓柱的平滑度（建議 16-64）

### 使用框選
1. 按住 **Shift** 鍵
2. 在空白處按下滑鼠左鍵
3. 拖拉出選擇框
4. 釋放滑鼠，框內物體被選中

### 使用 ViewCube
1. 找到右下角的白色方塊
2. 點擊方塊的任意面：
   - **面**：切換到該方向的正視圖
   - **邊**：切換到兩個方向的組合視角
   - **角**：切換到三個方向的組合視角
3. 攝影機會平滑過渡到新視角

### 查看物體名稱
- 名稱自動顯示在物體上方
- 選中物體時名稱有黃色邊框
- 在屬性面板中修改名稱會即時更新

## 🎨 UI/UX 改進

### 工具列更新
```
[➕ 長方體] [➕ 圓柱體] [刪除] | [移動] [縮放] | [對齊 X] [對齊 Y] [對齊 Z] | [匯入] [匯出] [預覽]
```

### 屬性面板智能化
- **長方體**：顯示「寬、高、深」
- **圓柱體**：顯示「半徑、高度、分段」
- 自動適應不同幾何類型

### 視覺元素
- **座標軸**：紅（X）、綠（Y）、藍（Z）
- **ViewCube**：白色方塊，黑色邊框，藍色懸停
- **選擇框**：藍色虛線，半透明填充
- **名稱標籤**：黑色半透明背景，白色文字

## 🔧 技術實現

### 新增的類型
```typescript
type GeometryType = 'box' | 'cylinder';

interface Zone {
  // ... 其他屬性
  geometry: GeometryType;
  dimensions: [number, number, number]; 
  // box: [width, height, depth]
  // cylinder: [radius, height, segments]
}
```

### 新增的組件
- **SelectionBox**：框選功能組件
- **Html (from drei)**：3D 空間中的 HTML 標籤
- **GizmoHelper & GizmoViewcube**：視角控制方塊
- **AxesHelper**：座標軸輔助器

### 改進的邏輯
- **Transform 狀態管理**：追蹤變換狀態以禁用攝影機
- **幾何類型判斷**：根據 geometry 屬性渲染不同形狀
- **投影計算**：框選時計算物體在螢幕上的投影

## 📊 功能對比

| 功能 | Tinkercad | 我們的編輯器 | 狀態 |
|------|-----------|--------------|------|
| 長方體 | ✅ | ✅ | 完成 |
| 圓柱體 | ✅ | ✅ | 完成 |
| 移動控制 | ✅ | ✅ | 完成 |
| 縮放控制 | ✅ | ✅ | 完成 |
| ViewCube | ✅ | ✅ | 完成 |
| 座標軸 | ✅ | ✅ | 完成 |
| 框選 | ✅ | ✅ | 完成 |
| 物體名稱 | ✅ | ✅ | 完成 |
| 多選 | ✅ | ✅ | 完成 |
| 對齊 | ✅ | ✅ | 完成 |
| 旋轉 | ✅ | ⏳ | 待實作 |
| 複製/貼上 | ✅ | ⏳ | 待實作 |
| 群組 | ✅ | ⏳ | 待實作 |

## 🎯 使用場景

### 創建工廠佈局
1. 使用長方體創建建築物
2. 使用圓柱體創建儲罐、煙囪等
3. 使用框選快速選擇多個物體
4. 使用對齊功能整齊排列
5. 使用 ViewCube 從不同角度檢查

### 精確建模
1. 使用座標軸確定方向
2. 使用移動模式的軸向箭頭精確定位
3. 使用縮放模式調整尺寸
4. 在屬性面板中輸入精確數值

### 快速導航
1. 使用 ViewCube 快速切換視角
2. 使用滑鼠中鍵旋轉視角
3. 使用滾輪縮放
4. 使用右鍵平移

## 🐛 已修復的問題

1. ✅ 拖拉時攝影機跟著移動 → 變換時禁用 OrbitControls
2. ✅ 無法點擊物體選擇 → 改進點擊事件處理
3. ✅ 缺少視角指示 → 添加座標軸和 ViewCube
4. ✅ 無法框選 → 實作 Shift + 拖拉框選
5. ✅ 只支援長方體 → 添加圓柱體支援
6. ✅ 物體難以辨識 → 添加名稱標籤

## 🚀 性能優化

- **條件渲染**：Transform Controls 只在選中時渲染
- **事件優化**：框選使用高效的投影計算
- **記憶化**：Zone3D 組件使用 memo 優化
- **HTML 標籤**：使用 drei 的 Html 組件高效渲染

## 💡 使用技巧

1. **快速建模**：先用長方體搭建主體，再用圓柱體添加細節
2. **批量操作**：使用框選 + 對齊快速整理佈局
3. **視角切換**：使用 ViewCube 快速查看不同角度
4. **精確調整**：在屬性面板中輸入精確數值
5. **命名規範**：給物體起有意義的名稱方便管理

## 🎉 總結

現在你的 3D 工廠佈局編輯器已經具備了：
- ✨ 多種幾何形狀（長方體、圓柱體）
- 🎯 專業的變換控制
- 📦 高效的選擇工具
- 🧊 直觀的視角控制
- 🏷️ 清晰的物體標識
- 📐 完整的空間指示

開始創建你的 3D 工廠佈局吧！🏭
